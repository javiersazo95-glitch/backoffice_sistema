# Entorno de desarrollo local.
#
# Las contrasenas se exigen con la forma ${VAR:?mensaje}: si no estan definidas, docker compose
# falla con ese mensaje en vez de arrancar con un valor por defecto conocido y versionado.
# Copia .env.example a .env y rellenalo antes del primer uso.
services:
  postgres:
    image: postgres:16-alpine
    container_name: backoffice_postgres
    environment:
      POSTGRES_DB: ${DB_NAME:-backoffice_dev}
      POSTGRES_USER: ${DB_USERNAME:-backoffice_dev}
      POSTGRES_PASSWORD: ${DB_PASSWORD:?define DB_PASSWORD en backoffice/.env (ver .env.example)}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-backoffice_dev}"]
      interval: 10s
      timeout: 5s
      retries: 5

  pgadmin:
    image: dpage/pgadmin4
    container_name: backoffice_pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_EMAIL:-admin@repuestop.cl}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD:?define PGADMIN_PASSWORD en backoffice/.env (ver .env.example)}
    ports:
      - "5050:80"
    depends_on:
      postgres:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    container_name: backoffice_frontend
    environment:
      VITE_API_PROXY_TARGET: http://host.docker.internal:8080
      CHOKIDAR_USEPOLLING: "true"
      WATCHPACK_POLLING: "true"
    ports:
      - "${FRONTEND_PORT:-5173}:5173"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./frontend:/app
      - /app/node_modules

volumes:
  pgdata:
  uploads:
